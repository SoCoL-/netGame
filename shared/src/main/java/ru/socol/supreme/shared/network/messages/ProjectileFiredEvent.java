package ru.socol.supreme.shared.network.messages;

/**
 * Сервер -> все клиенты: какой-то дальнобойный юнит только что выстрелил
 * (см. GameServer.isRangedShooter — лучник, разведчик, штурмовик, турель;
 * у ближнего боя, воина и строителя, этого события нет вовсе). Урон уже
 * применён авторитетно на сервере в момент выстрела (хитскан, без
 * задержки) — это сообщение чисто косметическое, нужно клиенту только
 * чтобы нарисовать летящий снаряд (см. GameScreen). Момент попадания на
 * экране может не совпадать по времени с реальным моментом урона на
 * сервере на доли секунды — сознательное упрощение, оправданное тем, что
 * здесь это не влияет на исход боя, только на картинку.
 */
public class ProjectileFiredEvent {

    public float fromX;
    public float fromY;
    public float toX;
    public float toY;

    public ProjectileFiredEvent() {
    }
}
