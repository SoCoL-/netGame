package ru.socol.supreme.shared;

/**
 * Все "магические числа" игры, общие для всех юнитов и не зависящие от их
 * типа, собраны здесь (лимиты, тикрейт, размеры карты и зданий) — и на
 * сервере, и на клиенте. Скорость, урон и дальность атаки — то, чем
 * реально отличаются типы юнитов друг от друга — вынесены в units.json
 * (см. UnitDefinitions), не сюда.
 */
public final class GameConstants {

    private GameConstants() {
    }

    /** Всего 2 игрока в игре. */
    public static final int MAX_PLAYERS = 2;

    /** Суммарный лимит юнитов на всю игру (не на игрока, а на обоих вместе). */
    public static final int MAX_TOTAL_UNITS = 300;

    public static final int TCP_PORT = 54555;

    /** Пока не используется: весь трафик сейчас идёт по TCP (см. GameClient.connect() / GameServer.start()). */
    public static final int UDP_PORT = 54777;

    /**
     * Размер буферов KryoNet под сериализацию сообщений. Server()/Client()
     * без аргументов используют крошечные буферы по умолчанию (16384 байт
     * на запись и всего 2048 — на один объект). Этого хватало, пока
     * WorldSnapshot был маленьким, но с ростом числа юнитов и особенно
     * после того как в UnitSnapshot добавился pathPoints (для отладочной
     * отрисовки маршрута — см. GameScreen) один снапшот на несколько
     * десятков юнитов уже не помещается в 2048 байт: сериализация падает с
     * переполнением буфера (баг: воспроизводился на ~40 юнитах). На
     * MAX_TOTAL_UNITS с юнитами, огибающими препятствия по всей сетке,
     * оценка худшего случая — десятки килобайт; 1 МБ даёт большой запас.
     */
    public static final int NETWORK_WRITE_BUFFER_SIZE = 1024 * 1024;
    public static final int NETWORK_OBJECT_BUFFER_SIZE = 1024 * 1024;

    /** На каком расстоянии до цели юнит считается прибывшим и останавливается. */
    public static final float ARRIVE_THRESHOLD = 2f;

    /**
     * Условный радиус юнита — используется для отрисовки на клиенте и как
     * реальный радиус столкновений в CollisionSystem (два юнита ближе
     * 2×UNIT_RADIUS друг к другу считаются перекрывающимися и
     * расталкиваются). Одинаков у всех типов — в отличие от здоровья,
     * скорости, скорострельности, урона и дальности атаки/агрессии,
     * которые теперь заданы по типу в units.json (см. UnitDefinitions):
     * этот, в отличие от них, не про боевые характеристики, а про то,
     * какого юнит физически размера, и её вынос в данные пока не давал
     * бы ничего, кроме лишнего слоя.
     */
    public static final float UNIT_RADIUS = 10f;

    /**
     * Запас, на который Pathfinding "раздувает" препятствия (воду, здания)
     * при планировании пути — сверх их реального размера. Без этого A*
     * мог выбрать клетку, чей центр формально снаружи препятствия, но
     * ближе UNIT_RADIUS к его краю — CollisionSystem тут же вытолкнула бы
     * юнита обратно, а он (после доворота курса в MovementSystem) тут же
     * пошёл бы туда снова, зависнув в вечном топтании у стены. Запас чуть
     * больше UNIT_RADIUS — на случай, если юнит "прибыл" не точно в центр
     * клетки, а где-то в пределах ARRIVE_THRESHOLD от него.
     */
    public static final float PATH_CLEARANCE = UNIT_RADIUS + ARRIVE_THRESHOLD;

    /**
     * Порог "прибытия" к ПРОМЕЖУТОЧНОЙ точке обходного пути (не к конечной
     * цели) — см. MovementSystem.processEntity. PATH_CLEARANCE защищает от
     * зависания юнита у СТАТИЧНОГО препятствия (здания/воды), но не от
     * другого ЮНИТА: если юнит впереди по тому же обходному пути
     * остановился ровно в точке следующей путевой точки (или рядом),
     * CollisionSystem (минимальная дистанция 2×UNIT_RADIUS = 20 между
     * юнитами) не подпускает идущего следом ближе этого расстояния — а
     * обычный ARRIVE_THRESHOLD (2) настолько мал, что "прибытие" в эту
     * точку становится физически недостижимым, и юнит зависает навсегда в
     * паре шагов от неё, вместо того чтобы просто пойти на следующую точку
     * маршрута. Порог заметно больше 2×UNIT_RADIUS (с запасом), чтобы
     * следующий по тому же пути юнит гарантированно мог засчитать
     * промежуточную точку "достигнутой" и пойти дальше, даже упираясь в
     * стоящего там соседа. Для КОНЕЧНОЙ точки маршрута по-прежнему
     * используется точный ARRIVE_THRESHOLD — там важна точность (например,
     * точка подхода к стройке или место, куда реально кликнул игрок).
     */
    public static final float PATH_WAYPOINT_ARRIVE_THRESHOLD = UNIT_RADIUS * 2.5f;

    /**
     * Скорость доворота башни наземного юнита на цель, градусов в секунду
     * — см. TurretAimSystem/TurretComponent. Одна общая величина на все
     * наземные типы (как и UNIT_RADIUS), не заданная по типу в
     * units.json — это чисто косметический параметр (см. javadoc
     * TurretComponent, почему на исход боя не влияет), а не боевая
     * характеристика вроде урона/дальности, которым там самое место.
     * 240 — полный оборот за 1.5 сек, достаточно быстро, чтобы башня
     * успевала цеплять цель, но не мгновенно, чтобы поворот был заметен
     * на глаз.
     */
    public static final float TURRET_TURN_SPEED_DEGREES_PER_SECOND = 240f;

    /**
     * Размер карты по каждой оси. Юниты, здания и камера не выходят за эти
     * границы. Увеличена в 4 раза по каждой оси (было 2000x2000) — вместе
     * с ней в те же 4 раза увеличены вода, месторождения железа и отступ
     * автоспавна дома (см. ниже), чтобы вся относительная геометрия карты
     * (расположение препятствий и ресурсов друг относительно друга)
     * осталась в точности такой же, просто в более крупном масштабе, а не
     * съёжилась в маленький угол выросшей карты.
     */
    public static final float MAP_WIDTH = 8000f;
    public static final float MAP_HEIGHT = 8000f;

    /**
     * Препятствие посередине карты — вода. Блокирует ХОЖДЕНИЕ (юниты
     * обходят её по Pathfinding), но не дальность атаки — стрелять через
     * воду можно, это сознательное упрощение (нет отдельной проверки
     * "линии обзора" для боя, только для движения).
     */
    public static final float WATER_MIN_X = 2800f;
    public static final float WATER_MAX_X = 5200f;
    public static final float WATER_MIN_Y = 2800f;
    public static final float WATER_MAX_Y = 5200f;

    /**
     * Месторождения железа — по 2 на игрока, симметрично, в открытом
     * пространстве недалеко от его же дома/казармы (зазор от построек ~400
     * юнитов при текущем размере карты — те же ~100, что и раньше, но
     * пропорционально возросшие вместе с картой в 4 раза). Каждая запись —
     * {x, y}. В отличие от воды/зданий, НЕ препятствие для движения — юниты
     * спокойно проходят прямо через них, это просто точка на карте, а не
     * физический объект. Здание добычи (см. IRON_MINE_* ниже) можно
     * поставить только СЮДА, "прилипая" к одной из этих точек — см.
     * GameScreen (превью при постройке) и GameServer.handlePlaceIronMine
     * (серверная проверка).
     */
    public static final float[][] IRON_DEPOSITS = {
            {2000f, 800f}, {800f, 2000f},     // игрок 0
            {6000f, 7200f}, {7200f, 6000f},   // игрок 1
    };

    /**
     * Базовая вместимость хранилища ресурса — доступна игроку всегда,
     * даже без единого здания IRON_STORAGE/ELECTRICITY_STORAGE (иначе
     * добывающее здание было бы бесполезно до постройки первого
     * хранилища — слишком жёстко для старта). Здания-хранилища
     * добавляют к ней BuildingDefinitions.storageCapacityFor(type) за
     * каждое — см. ResourceExtractionSystem, она же считает итоговую
     * вместимость заново каждый тик. Раздельные константы, а не одна
     * общая: у ресурсов сильно разный масштаб потока (электростанция —
     * 150/сек, шахта железа — 20/сек), одно и то же число было бы или
     * тесным для электричества, или бессмысленно огромным для железа.
     */
    public static final float IRON_BASE_CAPACITY = 300f; // ровно стоимость одного стрелка по железу
    public static final float ELECTRICITY_BASE_CAPACITY = 2000f; // ~13 сек добычи на полной ставке электростанции

    /**
     * Допуск на погрешность округления float при проверке "хватает ли
     * ресурсов на этот тик" в ProductionSystem/BuildSystem — НЕ реальная
     * нехватка ресурса, а типичный дребезг плавающей точки на последнем
     * тике постройки: после сотен маленьких вычитаний remaining/resources
     * и tick-стоимость, которые математически должны совпасть ровно,
     * расходятся в 14-15 знаке (например, доступно 0.017644369971943520,
     * а по формуле нужно 0.017644369971944070 — на 16 порядков меньше,
     * чем сама стоимость постройки). Без допуска строгое сравнение
     * "хватает ли" иногда решает, что не хватает, ресурс не списывается,
     * remaining не уменьшается — и постройка зависает на этом самом тике
     * навсегда, так и не завершившись. 0.001 — на три порядка больше
     * типичного дребезга, но всё равно на много порядков меньше самой
     * скромной реальной стоимости (десятки единиц), так что реальную
     * нехватку ресурса этот допуск замаскировать не может.
     */
    public static final float RESOURCE_EPSILON = 0.001f;

    /** Базовая вместимость по типу ресурса — используется ResourceExtractionSystem вместо отдельных if/else на каждый тип. */
    public static float baseCapacityFor(ResourceType type) {
        return type == ResourceType.ELECTRICITY ? ELECTRICITY_BASE_CAPACITY : IRON_BASE_CAPACITY;
    }

    /** Размер клетки сетки для поиска пути (Pathfinding) — 8000/50 = 160x160 клеток. */
    public static final float PATH_GRID_CELL_SIZE = 50f;

    // Туман войны — своя сетка, отдельная от Pathfinding. Клетка увеличена
    // в те же 4 раза, что и карта (было 25, при карте 2000x2000) — ровно
    // чтобы FOG_GRID_WIDTH/HEIGHT (а значит и размер FogSnapshot, который
    // рассылается по сети на каждый снапшот) остались теми же 80x80
    // клеток, что и раньше, а не выросли в 16 раз вместе с площадью карты.
    // При новых дальностях обзора (после ×3 юнитам/×6 зданиям, см.
    // UnitDefinitions/BuildingDefinitions) самая маленькая — у разведчика,
    // 300 — даёт 6 клеток в поперечнике при клетке 100, самая большая — у
    // стрелка, 750 — даёт 15: то же качество окружности, что было у
    // старой сетки (клетка 25) на радиусах 70..250, просто в новом
    // масштабе. FOG_GRID_WIDTH/HEIGHT — целое число клеток на всю карту,
    // MAP_WIDTH/HEIGHT кратны 100, деление ровное.
    public static final float FOG_GRID_CELL_SIZE = 100f;
    public static final int FOG_GRID_WIDTH = (int) (MAP_WIDTH / FOG_GRID_CELL_SIZE);
    public static final int FOG_GRID_HEIGHT = (int) (MAP_HEIGHT / FOG_GRID_CELL_SIZE);
    // Через сколько секунд после потери видимости клетка снова закрывается
    // туманом — см. GameServer.updateFogOfWar.
    public static final float FOG_REVEAL_GRACE_PERIOD = 2f;

    /**
     * Раньше было единое время постройки юнита на всех — теперь у каждого
     * типа своё (UnitDefinitions.buildTimeFor, появилось вместе со
     * штурмовиком и его 12 секундами вместо стандартных 10). Оставлена
     * только как запасное значение на клиенте (GameScreen
     * .drawBuildingInfoPanel) для доли прогресс-бара — если по какой-то
     * причине тип строящегося юнита ещё не известен (снапшот пуст),
     * не должна использоваться сервером напрямую нигде.
     */
    public static final float UNIT_BUILD_TIME = 10f;

    /**
     * Сколько дополнительной скорости строительства/ремонта даёт КАЖДЫЙ
     * следующий строитель сверх первого — первый даёт 100% (обычная
     * скорость, множитель 1.0), второй ещё +10% (итого 1.1), третий ещё
     * +10% (1.2) и так далее, а не удвоение/утроение на каждого. Общая
     * константа для BuildSystem (стройка) и RepairSystem (ремонт) — то же
     * самое рассуждение верно для обеих: несколько строителей работают
     * быстрее, но не пропорционально своему числу.
     */
    public static final float EXTRA_BUILDER_SPEED_BONUS = 0.1f;

    /**
     * Электричество, которое стоит ЛЮБОЙ ремонт здания — фиксированная
     * сумма, не зависящая от степени повреждений (в отличие от железа,
     * см. javadoc RepairComponent.totalIronCost) и от типа здания.
     * Списывается равномерно за время ремонта, тем же приёмом, что и
     * ironCostFor/electricityCostFor стройки в BuildSystem.
     */
    public static final int REPAIR_ELECTRICITY_COST = 100;

    /**
     * Сколько секунд занимает ремонт КАЖДОГО процента повреждений здания
     * (GameServer.assignBuilderToRepair: totalTime = damagePercent *
     * это). Полностью разрушенное (условно 100%) здание чинилось бы
     * 10 секунд — тот же порядок, что и обычное время стройки с нуля
     * (BuildingDefinitions.buildTimeFor, у всех типов сейчас 10f).
     * Намеренно ОТДЕЛЬНАЯ константа, а не переиспользование buildTimeFor
     * самого здания: у дома (HOME) buildTime == 0 (он появляется сразу
     * готовым, не через ConstructionComponent) — при этом дом всё равно
     * может быть повреждён в бою и должен ремонтироваться, а
     * "damagePercent * 0" дало бы totalTime == 0 и деление на ноль в
     * RepairSystem.advanceRepair.
     */
    public static final float REPAIR_SECONDS_PER_PERCENT_DAMAGE = 0.1f;

    /**
     * Доля первоначальной стоимости железа погибшего юнита, которая
     * остаётся в его обломках (GameServer.spawnWreck) — реальное
     * значение каждый раз выбирается случайно между MIN и MAX, а не
     * фиксированным процентом, чтобы одинаковые по цене юниты не
     * оставляли одинаковые до единицы обломки.
     */
    public static final float WRECK_IRON_PERCENT_MIN = 0.4f;
    public static final float WRECK_IRON_PERCENT_MAX = 0.6f;

    /**
     * Сколько железа в секунду собирает с обломков ОДИН строитель
     * (ScavengeSystem.advanceCollection) — тот же порядок величины, что
     * и добыча железной шахты (BuildingDefinitions: extractionRate = 20
     * у IRON_MINE), чтобы сбор ощущался сравнимо полезным, а не
     * мгновенным и не бесполезно медленным. Несколько строителей на
     * одних обломках работают быстрее по тому же множителю
     * EXTRA_BUILDER_SPEED_BONUS, что и стройка/ремонт.
     */
    public static final float WRECK_IRON_COLLECT_PER_SECOND = 20f;

    /**
     * playerId, обозначающий "ничей" — владелец обломков юнита
     * (GameServer.spawnWreck/WreckComponent). Не совпадает ни с одним
     * настоящим playerId (0 или 1, см. MAX_PLAYERS), поэтому обломки не
     * считаются "своими" ни у одного игрока (isMine на клиенте, проверки
     * владения на сервере) и никак не участвуют в checkGameOver
     * (buildingIdByPlayer никогда не ссылается на обломки). Само по себе
     * несовпадение с playerId НЕ делает обломки "врагом" — было бы так,
     * автобой (AggroSystem) и ручная атака (GameServer.startAttackOrder/
     * GameScreen.isEnemy) считали бы их законной целью только потому,
     * что они "не мои". От этого их защищает отдельная явная проверка на
     * WreckComponent в каждом из трёх мест, а не значение этой константы.
     */
    public static final int NEUTRAL_OWNER_ID = -1;

    /**
     * Множитель урона по турели (BuildingType.TURRET/UnitType.TURRET) —
     * применяется в CombatSystem только когда ЦЕЛЬ атаки турель, не когда
     * турель сама атакует (её собственный урон — обычный
     * UnitDefinitions.damageFor(TURRET), без этого множителя). Балансная
     * правка по просьбе пользователя: турель не стоит ресурсов и не
     * занимает лимит юнитов (см. javadoc UnitType.TURRET), поэтому саму
     * турель сделали более уязвимой к урону, чтобы бесплатная стационарная
     * защита не была неубиваемой.
     */
    public static final float TURRET_DAMAGE_MULTIPLIER = 1.5f;

    /** Частота обновления симуляции на сервере. */
    public static final float SERVER_TICK_RATE = 1f / 30f;

    /** Частота рассылки снапшотов мира клиентам. Также используется клиентом как длительность интерполяции. */
    public static final float SNAPSHOT_RATE = 1f / 15f;
}
